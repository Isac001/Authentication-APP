import 'package:flutter/widgets.dart';

// Class containing a global key for the user creation form
class CreateUserKey {
  // Global key to manage the state of the user creation form
  static final GlobalKey<FormState> createUserFormKey = GlobalKey<FormState>();
}
